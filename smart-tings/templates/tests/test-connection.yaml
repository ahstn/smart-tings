apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smarttings.fullname" . }}-test-connection"
  labels:
    {{- include "smarttings.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smarttings.fullname" . }}:{{ 8123 }}']
  restartPolicy: Never
